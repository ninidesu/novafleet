export default function TripTimeline({events=[]}){const fallback=[{id:"created",title:"Trip created",timestamp:"Scheduled",description:"Trip and assignments were recorded.",severity:"Low"},{id:"assigned",title:"Vehicle and driver assigned",timestamp:"Scheduled",description:"Dispatch resources confirmed.",severity:"Low"}];return <div className="trip-timeline">{(events.length?events:fallback).map((event)=><div className="trip-timeline-item" key={event.id}><i/><div><strong>{event.title}</strong><span>{event.timestamp} / {event.severity}</span><p>{event.description}</p></div></div>)}</div>}

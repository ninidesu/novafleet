const labels={success:"Success",error:"Error",warning:"Warning",info:"Information"}; export default function ToastViewport({toasts,onDismiss}){return <div className="toast-viewport" aria-live="polite" aria-atomic="false">{toasts.map(toast=><section className={`toast toast-${toast.type}`} key={toast.id} role={toast.type==='error'||toast.type==='warning'?'alert':'status'}><span className="toast-icon" aria-hidden="true">{toast.type==='success'?'✓':toast.type==='error'?'!':'i'}</span><div className="toast-copy">{toast.title&&<strong>{toast.title}</strong>}<p>{toast.message}</p></div>{toast.dismissible&&<button type="button" onClick={()=>onDismiss(toast.id)} aria-label={`Dismiss ${labels[toast.type]} message`}>×</button>}</section>)}</div>}
import Button from "./Button.jsx";
export default function InlineError({title="Unable to complete the request",message="Something went wrong. Please try again.",variant="standard",onRetry,retrying=false,retryLabel="Try again",onDismiss,dismissLabel="Dismiss",className=""}){const compact=variant==="compact";return <section className={`inline-error ${compact?"inline-error-compact":""} ${className}`.trim()} role="alert"><span className="inline-error-icon" aria-hidden="true">!</span><div className="inline-error-copy"><strong>{title}</strong><p>{message}</p></div>{(onRetry||onDismiss)&&<div className="inline-error-actions">{onDismiss&&<button type="button" onClick={onDismiss} aria-label={dismissLabel}>{dismissLabel}</button>}{onRetry&&<Button type="button" variant="secondary" onClick={onRetry} disabled={retrying}>{retrying?"Retrying…":retryLabel}</Button>}</div>}</section>}